{
  "name": "kibotalk",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev:api": "pnpm -F @kibotalk/api dev",
    "dev:landing": "pnpm -F @kibotalk/landing dev",
    "dev:web": "pnpm -F @kibotalk/web dev",
    "dev:desktop": "pnpm -F @kibotalk/desktop dev",
    "dev:playground": "pnpm -F @kibotalk/playground dev",
    "dev:ui": "pnpm -F @kibotalk/ui-kit story:dev",
    "dev:prototypes": "pnpm -F @kibotalk/playground exec vite ../../prototypes --host --port 5175",
    "eval": "vieval run --config ./vieval.config.ts --report-out .vieval/reports --workspace local --experiment multi-turn-ruby --attempt attempt-a",
    "eval:report": "vieval report analyze .vieval/reports --format json",
    "benchmark:drama": "tsx tools/drama-benchmark.ts",
    "benchmark:speaker:prepare": "tsx tools/prepare-speaker-trials.ts",
    "benchmark:speaker": "tsx tools/speaker-model-benchmark.ts",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@alint-js/agent-apeira": "0.2.0",
    "@alint-js/cli": "0.2.0",
    "@alint-js/plugin-js": "0.2.0",
    "@alint-js/plugin-simplicity": "0.2.0",
    "@kibotalk/conversation": "workspace:*",
    "@kibotalk/prompts": "workspace:*",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^22.20.1",
    "@xsai/generate-text": "0.4.4",
    "ffmpeg-static": "5.3.0",
    "tailwindcss": "^4.3.3",
    "tsx": "4.19.0",
    "turbo": "^2.5.0",
    "typescript": "^5.7.0",
    "vieval": "0.0.12",
    "vite": "^6.4.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "electron",
      "ffmpeg-static"
    ]
  }
}
